# Gauntlet Gap Matrix (P01-G02)

**Status:** record (process level, regenerated by hand per gauntlet)
**Scope:** per-crate coverage inventory: what exists, what was missing, what
this gauntlet added, what remains
**Related:** `docs/testing/coverage.sh` (LLVM numbers), `docs/evidence/P01-G02-*.md`

Line/function percentages are LLVM source-based coverage from the full suite
(see the coverage section of the evidence record). Coverage navigates; the
`remaining work` column states what would actually raise assurance.

## Per-crate matrix

| Crate | Existing coverage | Gap found | Implemented here | Remaining work |
|---|---|---|---|---|
| aipo-source (88/75) | spans, locations, CRLF/BOM | span/location properties, invalid-UTF-8 file path, line-span semantics | `tests/properties.rs` (6) | branch coverage of error paths |
| aipo-diagnostics (86/76) | emitter human/JSONL | UI goldens, suggestion mechanics, schema invariants | `tests/diagnostic_ui.rs` (via CLI), `tests/suggestions.rs` (3) | producer-side suggestions (no producer exists — feature, not test gap) |
| aipo-lexer (96/100) | classification, strings | arbitrary-UTF-8 totality, span monotonicity, EOF stability | `tests/properties.rs` (3) | nesting-limit interplay (covered at parser level) |
| aipo-ast (95/100) | type shapes | none material (data crate) | — | — |
| aipo-syntax (82/73) | parser + recovery (13) | host-abort on deep nesting; AST-depth hazards; arbitrary-token totality; placeholder span parity | depth guards + `depth.rs` + `shift.rs` + quadratic f-string fix; `tests/totality.rs` (2); ADP-005 | HIR/sema-side recursion audit if new recursive shapes land |
| aipo-hir (94/100) | lowering | none found beyond depth (fixed at syntax) | — | — |
| aipo-sema (89/93) | scopes, arity | contract-matrix program coverage | covered via `aipo-vm` contracts matrix + corpus | interface-satisfaction negative programs |
| aipo-ir (84/94) | lowering | **P0 panic**: hidden-receiver slot lookup on construction inside parameterized fns (fuzz-found) | fixed via `hidden_name`; regression program 23 | — |
| aipo-bytecode (88/86) | emitter/verifier/disasm | silent `u16`/`u8` truncation; unknown-function sentinel; hostile corpus | fallible `emit` + 3 unit tests; `tests/hostile.rs` (11) | disassembler exotic-opcode goldens (62% file) |
| aipo-vm (84/82) | faults, pipeline, core (35) | runtime arity last-line-of-defense untested (mutant survived!); value edges; contract strictness | arity regression test; `value_properties.rs` (7); `contracts_matrix.rs` (2); DictMap index + 5 unit tests | helpers.rs branches (63%), value fault branches; fuel/budgets → ADP-003 |
| aipo-runtime (92/89) | graph (10) | error-variant Display/codes untested | +1 graph test | — |
| aipo-stdlib (84/95) | functions (32) | unicode property matrix; math/prelude error branches (~60% files) | `unicode_properties.rs` (10) | error-branch unit tests for math/prelude |
| aipo-formatter (94/97) | golden, idempotency | fmt→reparse behavioral equivalence | `aipo-cli/tests/fmt_equiv.rs` (2) | — |
| aipo-cli | conformance 13, fuzz 5, build 3 | grammar fuzz; run-path fuzz with Unix-only `timeout`; resource exhaustion; UI goldens; examples rot (03/04 broken!); determinism | grammar + portable run fuzz; `resource.rs` (9); `diagnostic_ui.rs` (4); `examples.rs` (1); `determinism.rs` (2); fixed examples 03/04 | — |
| aipo-js | emitter, differential, properties | signed-zero display; int −0 normalization; source-map depth; shim unit properties | two parity fixes; `sourcemap.rs` (4); `selftest.mjs` extensions; `generated.rs` (2); `metamorphic.rs` (4) | shim fault-location mapping (needs shim map — documented limitation) |
| aipo-testkit (new) | — | shared harnesses | Rng, corpus, pipeline, js, proc (portable watchdog), timez, meta, smith + validity tests | struct/contract generator phases |

## Test-evidence drift check (this goal)

- `PROJECT_STATE.md` "152 workspace tests" for P01-G01: historical evidence,
  left untouched (allowed).
- `docs/conformance/README.md` inventory said 20/20 programs: updated to 23/23
  (programs 21–23 added by hardening work).
- `Cargo.toml` repository URL said `github.com/aipo-lang/aipo`: corrected to
  `github.com/raillen/aipo-lang`.
- P01-G01 evidence "20/20" counts: historical, untouched.
- Crate contracts `aipo-vm` Owns claimed fuel accounting: corrected to the
  stack-depth limit + ADP-003 pointer. Stale `gc-arena` mentions corrected
  earlier (kept correct).
