fn factorial(n)
    if n <= 1
        return 1
    end
    return n * factorial(n - 1)
end

fn fib(n)
    if n < 2
        return n
    end
    return fib(n - 1) + fib(n - 2)
end

each i in 1..6
    io.println(factorial(i))
end

each i in 0..8
    io.println(fib(i))
end
